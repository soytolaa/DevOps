apiVersion: v1
kind: Pod
metadata:
  name: nginx-app
  labels:
    app.kubernetes.io/name: nginx-app
spec:
  containers:
  - name: nginx-app
    image: nginx:alpine
    ports:
      - containerPort: 80
